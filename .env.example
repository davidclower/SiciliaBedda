# Contact Form Configuration

# Recipient email address
CONTACT_EMAIL=info@siciliabedda.com

# Email service to use: 'sendgrid', 'mailgun', 'smtp', 'webhook', or 'log' (for development)
EMAIL_SERVICE=log

# SendGrid Configuration (if using SendGrid)
SENDGRID_API_KEY=your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=noreply@siciliabedda.com

# Mailgun Configuration (if using Mailgun)
MAILGUN_API_KEY=your_mailgun_api_key_here
MAILGUN_DOMAIN=your_domain.com
MAILGUN_FROM_EMAIL=noreply@your_domain.com

# SMTP Configuration (e.g. Google Workspace for info@siciliabedda.com)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=info@siciliabedda.com
SMTP_PASS=your_google_app_password
SMTP_FROM="Sicilia Bedda Contact Form" <info@siciliabedda.com>

# Webhook Configuration (if using webhook - Zapier, Make.com, etc.)
WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/your_webhook_id

# Stripe (payments on Support Our Cause: donations + Heritage Collection)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
# Optional: base URL for success/cancel redirects (defaults to request origin)
# BASE_URL=https://yoursite.com

# Server Configuration
PORT=3000
